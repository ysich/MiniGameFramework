using System.Collections;
using System.Collections.Generic;
using Core.Module.UI;
using UnityEngine;

public class InnerLoginMainView : MainView
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
